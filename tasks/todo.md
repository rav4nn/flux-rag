# FluxRAG — Active Tasks

## Current Phase: 1 — Ingestion Layer ✅

Phase 0 complete. All ABCs defined, config models implemented, CLI/API stubs in place.
Phase 1 complete. All 9 parsers implemented + router + validator. 37 tests passing.

## Next: Phase 2 — Baseline

Ingest coffee corpus via new ingestion layer. Implement fixed chunking (Strategy A) + dense retrieval (Strategy 1). Run RAGAS with all-MiniLM-L6-v2. Establish baseline.

Done when: Baseline numbers in `eval/baseline_report.md`.
